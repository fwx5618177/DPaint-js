{
  "name": "dpaint-legacy",
  "version": "0.2.0",
  "private": true,
  "description": "Legacy plain-JS DPaint.js app — pre-migration reference, kept buildable.",
  "type": "module",
  "scripts": {
    "build": "vite build --base=./",
    "start": "http-server -p 8080 .",
    "test": "playwright test --config ./tests/playwright.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "http-server": "^14.1.1",
    "playwright": "^1.58.2",
    "vite": "^5.4.21"
  }
}
